enum Collections {
  users = 'users',
  templates = 'templates',
  invitations = 'invitations',
  events = 'events',
  subscriptions = 'subscriptions',
  tokens = 'tokens',
  payments = 'payments',
  plans = 'plans',
}

export default Collections;
